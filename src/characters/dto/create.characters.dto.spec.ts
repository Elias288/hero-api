import { CreateCharactersDto } from './create.characters.dto';

describe('CreateCharactersDto', () => {
  it('should be defined', () => {
    expect(new CreateCharactersDto()).toBeDefined();
  });
});
